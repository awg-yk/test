/**
 * exporter.js
 * ------------------------------------------------------------
 * [フェーズ4で実装予定]
 * 現在絞り込まれている観測所リストを CSV として書き出す機能、
 * および気象庁の「過去の気象データ・ダウンロード」ページへの
 * ディープリンクを生成する機能をここに実装する。
 *
 *   exportStationsAsCSV(stations: Station[]) -> void（ダウンロード実行）
 *   buildJmaDownloadUrl(stations: Station[]) -> string（要仕様調査）
 */

export function exportStationsAsCSV(/* stations */) {
  // TODO(フェーズ4): Blob + <a download> でCSVエクスポートを実装
  console.info("[exporter] フェーズ4で実装予定です。");
}
